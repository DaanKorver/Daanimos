import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    red: {
        color: '#f00',
    },
    container: {
        backgroundColor: '#eee',
        padding: 24
    },
});

export {styles};
