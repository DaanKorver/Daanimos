import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '800px',
        backgroundColor: '#EEE'
    },
    red: {
        color: '#f00',
        fontSize: 50,
    },
});

export {styles};
